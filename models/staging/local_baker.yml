version: 2

sources:
    - name: local_baker_source_data
      database: postgres
      schema: raw_datasets
      tables:
        - name: production_data
          description: Daily production amounts for the different product types offered by local baker
        - name: sales_data
          description: Daily sales amounts alongside recorded losses and workers pocket allowance.